version: 2

models:
  - name: fct_orders
    description: "Minimal Lightdash definition (core)"
    meta:
      lightdash:
        dimensions:
          total_revenue:
            type: number
            sql: ${order_total}
          ordered_at:
            type: timestamp
            sql: ${ordered_at}
        measures:
          gmv_running_total:
            type: running_total
            sql: ${total_revenue}
