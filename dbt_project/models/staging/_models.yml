version: 2

models:
  - name: stg_customers
    description: "Minimal Lightdash definition (staging)"
    meta:
      lightdash:
        dimensions:
          customer_id:
            type: number
            sql: ${customer_id}
          customer_name:
            type: string
            sql: ${customer_name}
